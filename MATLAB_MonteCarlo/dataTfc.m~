clearvars
close all
clc

% Number of copies
c = 4;

% Generate uniformly src & dst
xSrc = rand;
ySrc = rand;
xDst = rand;
yDst = rand;

% Find the corner of the square the src exists in
xCorner = floor(xSrc/sqrt(c));
yCorner = floor(ySrc/sqrt(c));

% Find the center of the square
xCenter = xCorner + 1/sqrt(c);
yCenter = yCorner + 1/sqrt(c);

% Find the distance from src to center & center to dst
dist = sqrt((xSrc - xCenter)^2 + (ySrc - yCenter)^2) + ...
    sqrt((xCenter - xDst)^2 + (yCenter - yDst)^2);

   
